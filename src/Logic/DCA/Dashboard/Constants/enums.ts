export enum ActionType {
  terminate = 1,
  modify = 2,
}

export enum PositionActions {
  active = 'ACTIVE',
  terminated = 'TERMINATED',
  completed = 'COMPLETED',
  swapped = 'SWAPPED',
  modified = 'SWAPPED',
}
